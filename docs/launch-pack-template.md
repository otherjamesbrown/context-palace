# Task Launch Pack Template

## Purpose

This template defines the candidate product artifact:

**a minimal task-launch working set that gets an AI coding agent to the first correct subsystem plus executable check with fewer wrong turns.**

Use this when testing whether a pre-assembled launch pack is materially better than a strong launcher.

## Task

```text
<short task description>
```

## Primary Objective

- get to:
  - <first correct subsystem plus executable check>

## Why This Pack Exists

- dominant risk:
  - <wrong subsystem / stale prose detour / proof path missing>
- why a thin launcher is incomplete here:
  - <one or two sentences>

## Source Of Truth

- proof surfaces:
  - <code paths>
  - <tests or nearest executable evidence>
- prose role:
  - routing / summary / warning only

## First Credible Route

- likely subsystem:
  - <name>
- likely code entrypoints:
  - <path 1>
  - <path 2>
- nearest executable evidence:
  - <test / command / script / repro path>

This section should represent the best first proof path, not just a list of candidates.

## Disambiguation

- ambiguity:
  - <what could send the agent in the wrong direction?>
- how to disambiguate quickly:
  - <specific file / check / question / signal>

## Trust Guidance

- likely current:
  - <doc/shard/note if any>
- use with caution:
  - <doc/shard/note if any>
- why:
  - <brief explanation>

## Minimal Supporting Prose

- note 1:
  - <small amount of domain context if needed>
- note 2:
  - <small amount of domain context if needed>

This section should be empty or tiny when prose is not necessary.

## First Three Moves

1. <open or inspect this path/check first>
2. <inspect or run this evidence second>
3. <only then consult this supporting prose or secondary path>

## Stop Conditions

- if this evidence disproves the route:
  - <where to pivot next>
- if the warning is confirmed:
  - <what should be distrusted or deprioritized>

## Why This Should Beat The Launcher

- pre-assembled route:
  - <how this reduces stitching>
- earlier proof linkage:
  - <how this reduces wandering>
- earlier trust calibration:
  - <how this reduces stale-doc detours>

## Notes

This pack should be:

- small
- task-specific
- proof-first
- explicit about ambiguity

It should not become:

- a mini design doc
- a broad repo overview
- a long ranked search result list
