## 🎯 Your Task: Analyze PRD for `spec-kit`

Please act as a product analyst. Your goal is to read the PRD, Design-Decisions, architecture docs provided below and distill it into two separate, clean descriptions that can be used for the `spec-kit` workflow.

1.  **A `/speckit.specify` description:**
    * This output must focus *only* on the **functional requirements**.
    * It should describe *what* the application does, the user stories, and the intended features from a user's perspective.
    * **Crucially, omit all technical implementation details** (like specific languages, frameworks, databases, or libraries).

2.  **A `/speckit.plan` description:**
    * This output must focus *only* on the **technical implementation**.
    * It should describe *how* the application will be built, including the technology stack, architecture, database choices, libraries, and any other non-functional or technical constraints.

Please generate the final, copy-and-paste-ready text for both commands based on the PRD.
